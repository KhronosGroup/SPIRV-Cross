#include <metal_stdlib>
#include <simd/simd.h>

using namespace metal;

struct Payload
{
    int x;
};

constant uint3 gl_WorkGroupSize [[maybe_unused]] = uint3(1u);

[[object]] void main0(mesh_grid_properties spvMpg, object_data Payload& payload [[payload]])
{
    payload.x = 0;
    spvMpg.set_threadgroups_per_grid(uint3(1u, 2u, 3u));
}

